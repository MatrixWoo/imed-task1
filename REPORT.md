# iMED-PE Task 1 进展汇报

> 汇报对象：师兄 · 日期：2026-08-16

## 一句话总结

在官方 baseline 上做四阶段方法改进（时序平滑 → 立体 PnP → 3D-3D 精修 → Kalman+RTS 平滑），
train mean ATE **2.163 → 1.098 mm（-49.2%）**，test **2.508 → 1.301 mm（-48.1%）**，
两个 split 改善一致，无过拟合。

## 一、已完成工作

### 1. Baseline 复现

- 官方 ALIKED + LightGlue + E 矩阵管线跑通
- train mean ATE = 2.163 mm，与 README 参考值 2.18 mm 吻合 ✅
- 100% 帧注册，61 train + 19 test 数据完整性验证通过

### 2. 四阶段方法改进

| 阶段 | 方法 | train ATE | 累计改善 |
|------|------|-----------|---------|
| baseline | E 矩阵（逐帧独立估计） | 2.163 | — |
| ① | 时序平滑：高斯窗口（w=2, σ=2）对四元数/平移加权平均，恢复首帧恒等约定 | 1.760 | -18.6% |
| ② | 立体三角化 + PnP：聚合 e1 固定基线，3D 点度量尺度跨帧一致，PNP RANSAC 求解 | 1.547 | -28.5% |
| ③ | 3D-3D Umeyama 精修：用 e2 立体深度独立重建同批点，闭式对齐精修位姿 + 双层质量门闸 | 1.271 | -41.2% |
| ④ | 恒定速度 Kalman + RTS 平滑：匀速模型 + 前向后向平滑，替代固定高斯窗（离线调参） | **1.098** | **-49.2%** |

**完整指标（最终版）**：

| 指标 | train baseline | train 最终 | test baseline | test 最终 |
|------|---------------|-----------|---------------|-----------|
| Mean ATE (mm) | 2.163 | **1.098** | 2.508 | **1.301** |
| Median ATE (mm) | 2.018 | **1.031** | 2.298 | **1.220** |
| RPE δ=1 trans (mm) | 1.164 | **0.597** | 1.426 | **0.738** |
| RPE δ=1 rot (deg) | 5.903 | **0.690** | 5.438 | **0.574** |
| RPE δ=10 rot (deg) | 7.402 | **2.088** | 7.584 | **1.928** |
| Registered | 100% | 100% | 100% | 100% |

### 3. 核心洞察：zoom-in 序列的病根是尺度漂移

- E 矩阵每帧独立估计 → 平移只有单位范数 → 帧间尺度不连续
- zoom-in 序列（大尺度变化）受害最重：最差序列 ATE 10.75 mm
- 立体基线固定后尺度全局一致：10.75 → 2.02（PnP）→ **<1 mm（精修）**
- 次差序列：5.59 → 2.58 → **1.16 mm**

### 4. 三个负结果（write-up 中如实报告）

| 尝试 | 结果 | 结论 |
|------|------|------|
| 逐帧 PnP/E 选择阈值（全局扫描 0.05–0.50 + GMM 自监督） | 净零和（1.547 vs 1.547） | 放弃，PnP 可用就用 |
| e2 深度一致性打分（交叉验证坏帧检测） | 残差恒好（中位 1.8%），无法区分坏帧 | 改为让 e2 深度**参与估计**（→ 阶段 ③） |
| LoFTR 密集匹配器 | 纯 E 路径 3 条里赢 2 条（匹配数 3-7×），但配对式关键点与我们的共享关键点 PnP 架构不兼容，全链路 7/8 输 | 放弃集成，保留 ALIKED |

### 5. 交付物

- `METHOD.md` — 完整方法描述（英文，提交用）
- `figures/` — ATE 对比图 + 轨迹可视化
- `src/imcpe/` — 最终代码（已同步 `imedpe_submission/`）
- GitHub：待推送

## 二、下一步计划（9/1 截止）

| 优先级 | 方向 | 预期收益 | 风险 |
|--------|------|---------|------|
| 1 | Docker 打包 + local_test + Synapse 提交（挂代理构建） | 保底分数 | 低 |
| 2 | Kalman 过程噪声自适应（按序列运动幅度调 q，当前固定值在 4 条快运动序列微退 <0.3 mm） | 小 | 低 |
| 3 | 轨迹级优化（pose-graph / 局部 bundle adjustment，用 e2 精修残差做约束） | 未知 | 中 |

时间安排：本周内完成首次提交拿榜单分，之后视时间决定是否做 v2 改进。

## 三、待讨论问题

1. 剩余的低频旋转漂移（δ=10 ≈ 2°，但 Sim(3) 对齐下对 ATE 影响有限）还值得继续攻吗？
2. 轨迹级优化（pose-graph / bundle adjustment）方向是否值得投入，还是先保提交？
